Prerequisite: The base images must be available.
See ../docker/README.md for details

To build an Alpine image and run it
```
./setup alpine && ./exa
```

To build a Ubuntu Bionic image and run it
```
./setup bionic && ./exa
```
